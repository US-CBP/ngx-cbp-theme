import { CBPDatetimePipe } from './datetime.pipe';

describe('CBPDatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new CBPDatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
